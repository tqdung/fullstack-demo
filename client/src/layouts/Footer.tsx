import { Box, Container } from '@chakra-ui/react';

export default function Footer() {
    return (
        <Box as="footer">
            <Container>
                Footer
            </Container>
        </Box>
    );
}